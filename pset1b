#M>I>T Introduction to computer science and programming using python 6.0001
#problem set 1(b) solution

annual_salary = float(input('Enter your annual salary: '))
portion_saved = float(input('Enter the portion of your monthly salary to be saved, in decimal: '))
total_cost = float(input('Enter the cost of your dream home: '))
portion_down_payment = 0.25 * total_cost
current_savings = 0
num_of_months = 1
portion_saved_monthly = annual_salary / 12 * portion_saved
r = 0.04  #annual rate on investment
increment = (current_savings * r / 12) + portion_saved_monthly
current_savings = current_savings + portion_saved_monthly
semi_annual_raise = float(input('Enter your semi annual raise, in decimal: '))
monthly_salary = annual_salary / 12
ho = 0.0001
#semi annual raise increases every 6 months
while current_savings < portion_down_payment:
   current_savings += current_savings * r/12 + portion_saved_monthly
   num_of_months += 1
   if num_of_months % 6 == 0:
     annual_salary += semi_annual_raise * annual_salary  #i'm getting wrong answers over here
print ('The number of months it\'ll take to save up for a down payment is : ', num_of_months)
#Tis' isn't correct or complete , will update when  i'm done .. 
#stil new to all this
